---
"@padosoft/utilities": minor
"@padosoft/react": minor
---

Fix `Configuration` `this` binding lost when passed to `useSyncExternalStore`, add optional overrides parameter to constructor, add `ReactiveConfiguration` subclass with built-in `useConfig` hook method and new overrides methods.

---
"@padosoft/react": minor
---

Add `genericMemo` typed wrapper around `React.memo` with better generic inference.

Also re-publishes with properly resolved workspace dependencies (`workspace:^` → real semver) now that the CI pipeline uses `bun pm pack`.
